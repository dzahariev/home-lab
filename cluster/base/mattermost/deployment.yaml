apiVersion: apps/v1
kind: Deployment
metadata:
  name: mattermost
  namespace: mattermost
  labels:
    app: mattermost
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: mattermost
  template:
    metadata:
      labels:
        app: mattermost
    spec:
      securityContext:
        fsGroup: 2000
      containers:
      - name: mattermost
        image: mattermost/mattermost-team-edition:11.8.0
        ports:
        - containerPort: 8065
          name: http
        securityContext:
          allowPrivilegeEscalation: false
        env:
        - name: TZ
          value: "Europe/Sofia"
        - name: MM_SQLSETTINGS_DRIVERNAME
          value: "postgres"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: mattermost-db-secrets
              key: db-user
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mattermost-db-secrets
              key: db-password
        - name: DB_PORT
          valueFrom:
            secretKeyRef:
              name: mattermost-db-secrets
              key: db-port
        - name: DB_NAME
          valueFrom:
            secretKeyRef:
              name: mattermost-db-secrets
              key: db-name
        - name: MM_SQLSETTINGS_DATASOURCE
          value: "postgres://$(DB_USER):$(DB_PASSWORD)@mattermostdb:$(DB_PORT)/$(DB_NAME)?sslmode=disable&connect_timeout=10"
        - name: MM_BLEVESETTINGS_INDEXDIR
          value: "/mattermost/bleve-indexes"
        - name: MM_SERVICESETTINGS_SITEURL
          value: "https://mattermost.example.com"
        - name: MM_LOGSETTINGS_CONSOLELEVEL
          value: "WARN"
        volumeMounts:
        - name: data
          mountPath: /mattermost/config
          subPath: config
        - name: data
          mountPath: /mattermost/data
          subPath: data
        - name: data
          mountPath: /mattermost/logs
          subPath: logs
        - name: data
          mountPath: /mattermost/plugins
          subPath: plugins
        - name: data
          mountPath: /mattermost/client/plugins
          subPath: client/plugins
        - name: data
          mountPath: /mattermost/bleve-indexes
          subPath: bleve-indexes
        - name: tmp
          mountPath: /tmp
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: mattermost-data
      - name: tmp
        emptyDir: {}
