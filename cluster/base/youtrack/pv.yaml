apiVersion: v1
kind: PersistentVolume
metadata:
  name: youtrack-data-pv
spec:
  capacity:
    storage: 500Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  claimRef:
    namespace: youtrack
    name: youtrack-data
  storageClassName: nfs-storage
  nfs:
    server: <nfs-server>
    path: /data/youtrack
