apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: taskboard-tasks
  namespace: taskboard
spec:
  storageClassName: local-storage
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 500Gi
