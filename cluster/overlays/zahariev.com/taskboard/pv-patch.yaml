apiVersion: v1
kind: PersistentVolume
metadata:
  name: taskboard-tasks-pv
spec:
  local:
    path: /media/tasks
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - atlas
