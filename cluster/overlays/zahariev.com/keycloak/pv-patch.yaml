apiVersion: v1
kind: PersistentVolume
metadata:
  name: keycloak-db-pv
spec:
  local:
    path: /data/keycloakdb
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - atlas
